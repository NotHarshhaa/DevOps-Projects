replicaCount: 2

image:
  repository: tushar31/spring-boot-app
  tag: "1"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

resources:
  requests:
    cpu: 40m
    memory: 128Mi
  limits:
    cpu: 100m
    memory: 220Mi
