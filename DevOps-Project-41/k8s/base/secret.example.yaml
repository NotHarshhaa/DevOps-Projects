# EXAMPLE ONLY — do not commit real secrets.
# Copy this file to secret.yaml and fill in base64-encoded values, or use
# an External Secrets Operator / Sealed Secrets integration instead.
apiVersion: v1
kind: Secret
metadata:
  name: ai-platform-secrets
  namespace: ai-devsecops
  labels:
    app.kubernetes.io/part-of: ai-native-devsecops-platform
type: Opaque
data:
  # echo -n 'Host=postgres;Database=aiops;Username=aiops;Password=CHANGEME' | base64
  POSTGRES_CONNECTION_STRING: <BASE64_ENCODED_VALUE>
  # echo -n 'CHANGEME' | base64
  OPENAI_API_KEY: <BASE64_ENCODED_VALUE>
